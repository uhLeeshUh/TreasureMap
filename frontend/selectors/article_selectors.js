// export const parseArticlesArray = (articlesArray) => {
//   let articles = {};
//   articlesArray.forEach(article => {
//     articles.article.id = article;
//   });
// };

//and then edit articles reducer to create the image_ids array for redux state
